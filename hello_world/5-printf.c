#include <stdio.h>

/**
 * main - Print a message on the program "with proper grammar, but the outcome
 *        is a piece of art,"
 * Return: Integer value 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

